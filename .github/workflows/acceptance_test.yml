# this is a generated file, DO NOT EDIT
# to generate this file run make pre-commit
# this is a generated file, DO NOT EDIT
# to generate this file run make pre-commit

name: Acceptance Tests
on:
  schedule:
    - cron: '30 19 * * *'
  workflow_dispatch:

env:
  ACC_TEST_CI: 1

jobs:
  createproject:
    name: Create Project
    runs-on: ubuntu-latest
    outputs:
      projectID: ${{ steps.create_project.outputs.ACC_TEST_PROJECT_ID }}
    steps:
      - name: Create test project
        id: create_project
        shell: bash
        run: |
          # JSON_DATA=$(cat << EOF
          # { 
          # "name": "${{ github.event.repository.name }}_${{ github.run_id }}", 
          # "containerParentId": "${{ secrets.ACC_TEST_PARENT_CONTAINER_ID }}", 
          # "members": [{ 
          # "role": "project.owner", 
          # "subject": "${{ secrets.ACC_TEST_USER_EMAIL }}" 
          # },{ 
          # "role": "project.owner", 
          # "subject": "${{ secrets.STACKIT_SERVICE_ACCOUNT_EMAIL }}" 
          # }], 
          # "labels": { 
          # "billingReference": "${{ secrets.ACC_TEST_BILLING_REF }}", 
          # "scope": "PUBLIC" 
          # }
          # }
          # EOF
          # )
          
          # curl --location --request POST 'https://api.stackit.cloud/resource-management/v2/projects' \
          #   --header 'Content-Type: application/json' \
          #   --header 'Accept: application/json' \
          #   --header 'Authorization: Bearer ${{ secrets.STACKIT_SERVICE_ACCOUNT_TOKEN }}' \
          #   --data-raw "${JSON_DATA}" > $HOME/pr.json
          
          # ACC_TEST_PROJECT_ID="$(jq -r '.projectId' $HOME/pr.json)"
          ACC_TEST_PROJECT_ID="a4d42390-a3fb-40a5-bbab-ecc3988f8091"
          echo "ACC_TEST_PROJECT_ID=${ACC_TEST_PROJECT_ID}" >> $GITHUB_ENV
          echo "ACC_TEST_PROJECT_ID=${ACC_TEST_PROJECT_ID}" >> $GITHUB_OUTPUT
          [[ -z "${ACC_TEST_PROJECT_ID}" || "${ACC_TEST_PROJECT_ID}" == "NULL" || "${ACC_TEST_PROJECT_ID}" == "null" ]] && exit 1 || exit 0

      - name: Wait project created
        id: wait_project_active
        uses: mydea/action-wait-for-api@v1
        with:
          url: 'https://api.stackit.cloud/resource-management/v2/projects/${{ env.ACC_TEST_PROJECT_ID }}'
          headers: '{
              "Accept": "application/json",
              "Authorization": "Bearer ${{ secrets.STACKIT_SERVICE_ACCOUNT_TOKEN }}"
            }'
          expected-response-field: 'lifecycleState'
          expected-response-field-value: 'ACTIVE'


  dsargus-instance:
    needs: createproject
    name: Test argus instance Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test argus instance Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/argus/instance

  dsargus-job:
    needs: [createproject,dsargus-instance]
    name: Test argus job Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test argus job Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/argus/job

  dsdata-services-credential:
    needs: createproject
    name: Test data-services credential Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test data-services credential Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/data-services/credential

  dsdata-services-instance:
    needs: [createproject,dsdata-services-credential]
    name: Test data-services instance Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test data-services instance Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/data-services/instance

  dskubernetes-cluster:
    needs: createproject
    name: Test kubernetes cluster Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test kubernetes cluster Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/kubernetes/cluster

  dskubernetes-project:
    needs: [createproject,dskubernetes-cluster]
    name: Test kubernetes project Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test kubernetes project Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/kubernetes/project

  dsmongodb-flex-instance:
    needs: createproject
    name: Test mongodb-flex instance Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test mongodb-flex instance Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/mongodb-flex/instance

  dsobject-storage-bucket:
    needs: createproject
    name: Test object-storage bucket Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage bucket Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/object-storage/bucket

  dsobject-storage-credential:
    needs: [createproject,dsobject-storage-bucket]
    name: Test object-storage credential Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage credential Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/object-storage/credential

  dsobject-storage-credentials-group:
    needs: [createproject,dsobject-storage-bucket,dsobject-storage-credential]
    name: Test object-storage credentials-group Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage credentials-group Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/object-storage/credentials-group

  dsobject-storage-project:
    needs: [createproject,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group]
    name: Test object-storage project Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage project Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/object-storage/project

  dspostgres-flex-instance:
    needs: createproject
    name: Test postgres-flex instance Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test postgres-flex instance Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/postgres-flex/instance

  dsproject:
    needs: createproject
    name: Test project Data Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test project Data Source
        shell: bash
        run: |
          make dummy PATH=stackit/internal/data-sources/project



  resargus-instance:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test argus instance Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test argus instance Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/argus/instance

  resargus-job:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject,resargus-instance]
    name: Test argus job Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test argus job Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/argus/job

  resdata-services-credential:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test data-services credential Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test data-services credential Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/data-services/credential

  resdata-services-instance:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject,resdata-services-credential]
    name: Test data-services instance Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test data-services instance Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/data-services/instance

  reskubernetes-cluster:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test kubernetes cluster Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test kubernetes cluster Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/kubernetes/cluster

  reskubernetes-project:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject,reskubernetes-cluster]
    name: Test kubernetes project Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test kubernetes project Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/kubernetes/project

  resmongodb-flex-instance:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test mongodb-flex instance Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test mongodb-flex instance Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/mongodb-flex/instance

  resobject-storage-bucket:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test object-storage bucket Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage bucket Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/object-storage/bucket

  resobject-storage-credential:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject,resobject-storage-bucket]
    name: Test object-storage credential Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage credential Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/object-storage/credential

  resobject-storage-credentials-group:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject,resobject-storage-bucket,resobject-storage-credential]
    name: Test object-storage credentials-group Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage credentials-group Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/object-storage/credentials-group

  resobject-storage-project:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject,resobject-storage-bucket,resobject-storage-credential,resobject-storage-credentials-group]
    name: Test object-storage project Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test object-storage project Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/object-storage/project

  respostgres-flex-instance:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test postgres-flex instance Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test postgres-flex instance Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/postgres-flex/instance

  resproject:
    needs: [dsargus-instance,dsargus-job,dsdata-services-credential,dsdata-services-instance,dskubernetes-cluster,dskubernetes-project,dsmongodb-flex-instance,dsobject-storage-bucket,dsobject-storage-credential,dsobject-storage-credentials-group,dsobject-storage-project,dspostgres-flex-instance,dsproject]
    name: Test project Resource
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - name: Prepare environment
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      - name: Test project Resource
        shell: bash
        run: |
          make dummy PATH=stackit/internal/resources/project


      # - name: Test
      #   id: acceptance-test
      #   run: make testacc ACC_TEST_BILLING_REF="${{ secrets.ACC_TEST_BILLING_REF }}" ACC_TEST_USER_EMAIL="${{ secrets.ACC_TEST_USER_EMAIL }}" STACKIT_SERVICE_ACCOUNT_TOKEN="${{ secrets.STACKIT_SERVICE_ACCOUNT_TOKEN }}" STACKIT_SERVICE_ACCOUNT_EMAIL="${{ secrets.STACKIT_SERVICE_ACCOUNT_EMAIL }}"

  deleteproject:
    name: Delete Project
    runs-on: ubuntu-latest
    needs: [resargus-instance,resargus-job,resdata-services-credential,resdata-services-instance,reskubernetes-cluster,reskubernetes-project,resmongodb-flex-instance,resobject-storage-bucket,resobject-storage-credential,resobject-storage-credentials-group,resobject-storage-project,respostgres-flex-instance,resproject]
    if: ${{ always() }}
    steps:
      - name: Prepare deletion
        id: prep_deletion
        if: always()
        shell: bash
        run: |
          echo "ACC_TEST_PROJECT_ID=${{needs.createproject.outputs.projectID}}" >> $GITHUB_ENV
      # - name: Delete test project
      #   id: delete_project
      #   if: always()
      #   shell: bash
      #   run:   |
      #     [[ -z "${ACC_TEST_PROJECT_ID}" || "${ACC_TEST_PROJECT_ID}" == "NULL" || "${ACC_TEST_PROJECT_ID}" == "null" ]] && exit 0
      #     echo "Deleting project ID: ${ACC_TEST_PROJECT_ID}"
      #     curl --location --request DELETE 'https://api.stackit.cloud/resource-management/v2/projects/${{ env.ACC_TEST_PROJECT_ID }}' \
      #       --header 'Accept: application/json' \
      #       --header 'Authorization: Bearer ${{ secrets.STACKIT_SERVICE_ACCOUNT_TOKEN }}'
